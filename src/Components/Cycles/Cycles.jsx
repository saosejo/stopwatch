import React from "react";
import Timer from "../Timer/Timer.css";

export default function Cycles(props) {
//   const minutes = Math.floor(props.time / 60000);
//   const seconds = Math.floor((props.time / 1000) % 60);
//   const milliseconds = Math.floor((props.time % 1000) / 10);

  // return (
  //   <div className="timer">
  //     <span className="digits">{props.cycles}</span>
  //   </div>
  // );
}